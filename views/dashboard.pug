extends layout

block content
    form.uk-container.uk-margin-top
        .uk-margin
            #uploader(uk-form-custom)
                input(type="file")
                button.uk-button.uk-button-default(type="button" tabindex="-1") Upload your video
        .uk-margin
            progress#progressbar.uk-progress(value="0" max="100" hidden)